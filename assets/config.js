/* config.js · 기준일과 도메인 상수
 *
 * BASE_DATE 는 이 파일에만 있다. 지난번에 기준일이 두 곳에 있어서
 * (connect_both.py 09-07 · engine.py 09-03) 마감일이 4일씩 어긋났다.
 * 다른 파일은 전부 window.CFG.BASE_DATE 를 읽는다. 날짜를 다시 적지 않는다.
 *
 * 산식 상수(Z · 가중치 · 선행일수)는 여기 두지 않는다. 그건 알고리즘 담당의
 * 명세이므로 db-derived.js 의 meta.spec 에서 읽는다. 여기 두면 두 곳이 갈린다.
 * 여기 있는 것은 "알고리즘 밖의 업무 규칙" 뿐이다.
 */
(function () {
  'use strict';

  window.CFG = {
    BASE_DATE: '2026-09-03',

    // 세션 기본 부서. 로그인에서 고른 값이 있으면 그것이 이긴다
    DEPT: 'SEO26FF',

    // 3층 저장소 키. db-changes.js 가 쓴다
    STORE_KEY: 'mtrl.db.v1',
    // 로그인이 고른 소속. org.js · nav.js 가 읽는다
    SESSION_KEY: 'mtrl.session.v1',

    // 재고 트랜잭션 4종과 부호.
    // qty 는 언제나 양수로 넣고 부호는 읽는 쪽이 종류로 정한다.
    // 수량 필드를 고쳐 쓰면 두 번 반납했을 때 무엇이 맞는지 알 수 없다.
    TXN_SIGN: { '반납': +1, '입고': +1, '불출': -1, '공용전환': -1 },

    // 승인할 수 있는 속성은 둘뿐이다.
    // 회색지대와 배제는 판단을 못 내린 것이라 승인 대상이 아니다.
    CONFIRMED_TYPES: ['보험품', '계획품'],

    // 판정 5종. 화면 표시는 LABEL 을 쓴다
    VERDICTS: ['보험품', '계획품', '회색지대', '배제-소모품', '배제-순환품'],
    // 「회색지대」 는 화면에서 「현행유지」 로 순화한다. 내부 값은 그대로 둔다.
    // 「일치 확인」 이 아니라 「판단 보류」 라는 뜻이므로 신뢰도를 함께 낸다
    LABEL: {
      '회색지대': '현행유지',
      '배제-소모품': '소모품',
      '배제-순환품': '순환품'
    },

    // 휴지구분 6종 (정비계획 kind)
    STOP_KINDS: ['합리화', '대수리', '중수리', '정기수리', '교체휴지', '공정휴지'],

    // 반납 창고. 유형마다 가는 곳이 다른 것이 현장 실수의 1번 원인이다
    RETURN_WH: {
      'QVC70': '중고품',
      'QVC75': '불용',
      'QVC77': '중앙야적장',
      'QVC01': '중앙창고', 'QVC02': '중앙창고', 'QVC03': '중앙창고',
      'QVC04': '중앙창고', 'QVC05': '중앙창고', 'QVC08': '중앙창고'
    },
    // 반납 거래코드
    RETURN_TXN: { 'Q00101': '신품반납', 'Q00201': '중고반납', 'T00301': '투자자재' },

    // PFC(QFC01) 업무 규칙
    PFC_WH: 'QFC01',
    PFC_DUE_DAYS: 30,      // 납기일 대비 사용예정일 30일 이상
    PFC_POOL_DAYS: 180,    // 입고 후 180일 초과 -> 공용자재 자동 전환
    CONSIGN_RETURN_DAYS: 3, // Consignment 불출 후 3일 이내 원 창고 리턴

    // 건강도 얼굴 경계. 색만으로 구분하지 않으므로 표정도 같이 정한다
    DUE_TONE: { fine: 7, soon: 3 }, // 7일 이상 fine · 3~6 soon · 2 이하 over

    // 좁은 화면에서 표를 카드로 펼칠 때 처음 그리는 장 수.
    // 743행을 다 그리면 문서 높이가 19만 px 이 된다 (실측)
    CARD_PAGE: 20,

    // 챗봇(사내 문서 RAG) 서버. 팀원이 주는 주소로 바꾼다.
    // 안 뜨면 화면이 시연 모드로 떨어진다
    CHAT_API: ''
  };
})();
