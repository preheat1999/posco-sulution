# 작업서 — 적정재고 산출 재현 가이드

> **이 폴더 하나로**: 설비 예비품(743종)의 적정재고·발주 신호등을 **내가 만든
> 기준과 동일하게 재현**할 수 있다. 새 데이터를 넣어도 같은 알고리즘으로 판단된다.
> 처음 보는 사람/AI가 **아래 순서대로만 따라 하면** 된다.

---

## 0. 이 폴더에 뭐가 있나 (파일 12개)

```
SHARE_appropriate_quantity2/
├─ 00_작업서.md          ← 지금 이 문서 (순서·방법)
├─ 01_알고리즘_명세.md    ← ★모든 공식·기준 (재현의 근거)
├─ 02_데이터_명세.md      ← 입력 CSV 컬럼 정의 + 새 데이터 만드는 법
├─ 03_연동_인터페이스.md   ← 속성분류 알고리즘과 연결하는 법
├─ engine.py             ← ★핵심 엔진 (로드→통계→중요도→적정재고, 단일파일)
├─ run_all.py            ← ★실행 진입점 (한 번에 모든 산출물 생성)
├─ _build_data.py        ← (원본→CSV 변환기, 재생성용. 보통 안 씀)
├─ data/
│   ├─ master.csv          자재 마스터 743종
│   ├─ txn_history.csv      불출/입고 이력
│   ├─ equipment_map.csv    자재↔설비, 설치수기준
│   └─ maintenance.csv      정비계획(설비 정지일정)
└─ output/                ← 실행하면 여기에 결과가 생김
    ├─ restock.csv          품목별 적정재고·발주필요·근거
    ├─ order_signals.csv    발주 신호등(🔴🟡🟢⚪)
    ├─ pooling.csv          정체 공용화 후보
    └─ SUMMARY.txt          요약 리포트
```

---

## 1. 준비물

- **Python 3.10 이상** (3.14에서 개발). 표준 라이브러리만 쓴다.
  **외부 패키지 설치 불필요** (pandas·numpy 등 안 씀).
- 그게 전부다. 인터넷·DB 접속도 필요 없다(데이터가 `data/` 에 이미 있음).

---

## 2. 실행 (딱 한 줄)

폴더 안에서 (PYTHONHASHSEED=0을 먼저 설정한다):
```powershell
$env:PYTHONHASHSEED = "0"
python -B run_all.py
```
```bash
PYTHONHASHSEED=0 python -B run_all.py
```
끝나면 `output/` 에 결과 4개가 생긴다. 화면에도 요약이 뜬다.

> 개별 확인이 필요하면 `python -B engine.py` 로 엔진만 돌려볼 수도 있다.

---

## 3. 결과 확인 — 이 숫자가 나오면 재현 성공

`output/SUMMARY.txt` 를 열어 아래와 대조:

| 항목 | 기대값 |
|---|---|
| 대상 자재 | 743종 (보험품 441 / 계획품 302) |
| 계획품 목표재고 | 약 25억 → 0.06억 (**−99.8%**) |
| 정체(공용화 후보) | 약 262종 |
| 재무효과(전사 확산) | 연 18.5억원 |
| 발주 신호등 | 🔴·🟡·🟢·⚪ 로 분류됨 |
| 중요도 등급 | S/A/B/C 4등급 |

> 실행 전 반드시 환경변수 `PYTHONHASHSEED=0` 을 설정해야 재현된다. 리드타임창
> 부트스트랩의 seed가 `abs(hash(품번)) % 99991` 로, 파이썬 문자열 해시가
> 프로세스마다 랜덤 솔트를 쓰기 때문이다(모듈 상단의 `SEED=42`는 실제로는
> 어디에도 쓰이지 않는 죽은 상수이니 착각하지 말 것). 이 환경변수를 고정하면
> 소수점 수준까지 재현되고, 안 하면 실행할 때마다 값이 흔들린다.

---

## 4. 새 데이터로 돌리고 싶다면

`data/` 의 CSV 4개를 새 데이터로 교체하면, **똑같은 알고리즘**으로 새 결과가
나온다. CSV 컬럼 규격은 `02_데이터_명세.md` 를 그대로 지켜야 한다.
- 최소한 `master.csv` 와 `txn_history.csv` 만 있으면 적정재고가 나온다.
- `maintenance.csv` 가 있으면 계획품 발주 신호등까지 나온다.
- `equipment_map.csv` 가 있으면 자재-설비 연결·설치수 보장이 반영된다.

> 원본이 CSV가 아닌 다른 형식(TSV 등)이면 `_build_data.py` 를 참고해 변환하라.

---

## 5. 알고리즘을 이해하고 싶다면

- **`01_알고리즘_명세.md`**: 왜 이렇게 판단하는지, 모든 공식(중요도 가중치,
  보험품 μ_LT+SS, 계획품 원칙0, 신호등 발주기준일)이 다 있다. 코드
  `engine.py` 와 1:1로 대응한다.
- 이 문서만 있으면 다른 언어로 재구현도 가능하다.

---

## 6. 내 속성분류 알고리즘과 연결하려면

당신이 각 자재의 **속성값(계획품/보험품)** 을 정하는 알고리즘을 만든다면:
- `master.csv`는 직접 고치지 않는다. 대신 `classification_result.csv`에
  `Qcode+DeptCode`별 판정을 저장하면, `보험품`/`계획품`으로 확정된 행만
  master의 Type을 override한다(회색지대·배제는 원본 Type 유지).
- 그 값에 따라 이 시스템이 자동으로 다른 공식으로 적정재고를 산출한다.
- 상세 규격: **`03_연동_인터페이스.md`**.

---

## 요약 (한 문장)

**`python -B run_all.py` → `output/SUMMARY.txt` 확인. 새 데이터는 `data/`
CSV 4개를 규격대로 교체하면 같은 기준으로 재현된다.**
