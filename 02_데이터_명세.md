# 데이터 명세 — 입력 CSV 5종 규격

> `data/` 의 CSV 5개가 엔진의 입력이다. 새 데이터로 재현하려면 아래 컬럼명·
> 형식을 그대로 지켜야 한다. 모두 **UTF-8(BOM), 쉼표 구분, 첫 행 헤더**.

---

## 1. classification_result.csv — 1단계 속성분류 결과 (선택)

1단계 알고리즘이 먼저 생성하는 연결 파일이다. 파일이 있으면 엔진이
`Qcode+DeptCode`가 정확히 일치하는 마스터 행의 `Type`만 메모리에서 갱신한다.
`master.csv` 자체는 덮어쓰지 않는다.

| 컬럼 | 필수 | 의미 |
|---|---|---|
| `Qcode` 또는 `Item` | ★ | 자재 코드 |
| `DeptCode` 또는 `부서코드` | ★ | 7자리 부서코드 |
| `Type` 또는 `판정` | ★ | `보험품` 또는 `계획품` |

`회색지대`, `데이터 부족`, `배제-소모품`, `배제-순환품` 등은 적정재고용
속성으로 적용하지 않고 기존 `master.csv` 값을 유지한다. 동일한
`Qcode+DeptCode`가 서로 다른 유효 Type으로 중복되면 실행을 중단한다.

예:

```csv
Qcode,DeptCode,Type
Q1000108,SEO26FF,보험품
```

## 2. master.csv — 자재 마스터 (★필수, 743행)

행 1개 = 자재 1종. 엔진이 가장 먼저 읽는다.

| 컬럼 | 필수 | 의미 | 예 |
|---|---|---|---|
| `Item` | ★ | 자재 고유코드(품번) | Q1053117 |
| `Type` | ★ | 속성값 (**보험품** 또는 **계획품**) | 보험품 |
| `DeptCode` | ★ | 분석 부서코드. `Qcode+DeptCode`가 분석 키 | SEO26FF |
| `CriticalSparePart` | ★ | 핵심예비품 여부 (O/X) | O |
| `CriticalEquipment` | ★ | 핵심설비자재 여부 (O/X) | X |
| `Warehouse` | | 보관 창고코드 | QFC01 |
| `SourcingGroup` | | 소싱그룹(구매 카테고리) | Q_Ball & Roller BRG |
| `StockDept` | ★ | 부서 현재고 수량 | 3 |
| `StockAll` | ★ | 전사 현재고 수량 | 5 |
| `SupplierCount` | | 실납품 공급사 수 | 2 |
| `UnitCost` | ★ | 단가(원) | 1250000 |
| `LeadTimeMean` | ★ | 리드타임 평균(일) | 67 |
| `LeadTimeStd` | | 리드타임 표준편차(일) | 20 |
| `CompletedCycles` | | 완결 조달사이클 수(LT 신뢰도) | 4 |
| `ProcurementType` | | 조달구분(국내/수입) | 수입 |
| `ReceivingDate` | | 최근 입고일(YYYY-MM-DD, 정체판정용) | 2022-03-15 |
| `LinkedEquipment` | | 연결 설비명(정비계획 연동용) | FM Main Motor |
| `SparePartHoldingStd` | | 핵심예비품 보유기준(설치수) | 1 |

> **최소 필수**: Item, Type, CSP/CEQ, StockDept/All, UnitCost, LeadTimeMean.
> 나머지는 없으면 기본값·중위값으로 대체된다(엔진이 자동 처리).

---

## 3. txn_history.csv — 불출/입고 이력 (★필수, 수요 원천)

행 1개 = 특정 자재·날짜·창고의 거래 집계.

| 컬럼 | 필수 | 의미 |
|---|---|---|
| `Item` | ★ | 자재 코드 (master.csv 와 매칭) |
| `DeptCode` | ★ | 부서코드 (master.csv 와 매칭) |
| `TXN_DATE` | ★ | 거래일자 (YYYY-MM-DD) |
| `SUBINV` | ★ | 창고코드 (부서창고 6개만 수요로 인정) |
| `QTY_IN` | | 입고 수량 |
| `QTY_OUT` | ★ | **불출 수량 = 수요** (핵심) |
| `ITEM_ID` / `ORG_ID` / `TXN_CNT` | | 참고 |

> 부서창고 6개: QFC01, QHB24, QHB25, QHB27, QVC03, QVC07.
> 그 외 창고 거래는 부서 수요에서 제외된다(엔진 필터).

---

## 4. equipment_map.csv — 자재↔설비 (선택, 있으면 정확도↑)

| 컬럼 | 의미 |
|---|---|
| `Item` | 자재 코드 |
| `DeptCode` | 부서코드 |
| `LinkedEquipment` | 연결 설비명 (maintenance.csv 의 Equipment 와 매칭) |
| `SparePartHoldingStd` | 핵심예비품 설치수기준(하한 보장에 사용) |

> master.csv 의 LinkedEquipment/SparePartHoldingStd 와 동일 정보. 별도 관리하면
> 이 파일로, 아니면 master 에만 있어도 된다.

---

## 5. maintenance.csv — 정비계획 (선택, 계획품 발주 신호등용)

행 1개 = 특정 설비의 한 수리 일정.

| 컬럼 | 필수 | 의미 |
|---|---|---|
| `Equipment` | ★ | 정지 대상 설비명 |
| `DeptCode` | ★ | 설비가 속한 부서코드 |
| `MaintKind` | ★ | 휴지구분(대수리/중수리/정기수리/합리화/교체휴지/공정휴지) |
| `StopStart` | ★ | 정지 시작일 (YYYY-MM-DD) |
| `ReStart` | | 재가동일 |
| `StopHours` | | 계획 정지시간(시간) — severity 계산 |
| `Manpower` | | 계획 투입인력 — severity 계산 |
| `Factory` / `Line` | | 공장/라인 |

> 미래 일정(오늘 이후)은 발주 신호등, 과거 일정은 예상소요량 추정·severity 에 쓴다.

---

## 6. 새 데이터로 재현하는 법

1. 위 규격대로 `data/` 의 CSV 4개를 새 값으로 교체하고, 1단계 결과를
   `classification_result.csv`로 넣는다. `data/` 폴더가 없으면 프로젝트
   루트의 CSV를 사용한다.
   - **최소** `master.csv` + `txn_history.csv` 만 있으면 적정재고가 나온다.
   - `maintenance.csv` 추가 → 계획품 발주 신호등까지.
2. `python -B run_all.py` 실행.
3. `output/` 결과 확인.

## 7. 원본이 다른 형식(TSV·한글컬럼)이면

`_build_data.py` 가 원본(한글 TSV 등) → 위 CSV 규격으로 변환하는 예시다.
컬럼 매핑만 자기 원본에 맞게 바꿔 재사용하면 된다.

## 8. 값 규칙 요약

- 불리언류(CSP/CEQ, 조달구분): `O`/`X`, `수입`/`국내` 문자열 그대로.
- 날짜: `YYYY-MM-DD`.
- 수량·금액: 숫자(쉼표 있어도 엔진이 제거). 빈 값은 결측 처리(기본값 대체).
- Type: 반드시 `보험품` 또는 `계획품`. (소모품·순환품은 이 시스템 대상 아님)
- 분석 키: 모든 파일에서 `Item(Qcode) + DeptCode` 조합으로 매칭한다.
