# 알고리즘 담당 → 동료 전달 목록 (본선 반출용)

본선 규칙: **코드 반출 불가. 문서·프롬프트·더미 데이터만 가능.**
그래서 파이썬 3개는 빼고, 그 코드를 새로 작성할 수 있는 규격을 문서에 채워 넣었다.

전달 파일은 **총 14개** (문서 6 + 데이터 8). 아래 표의 행수·md5로 도착 후 무결성을 확인한다.

---

## A. 문서 6개

| # | 파일명 | 위치 |
|---|---|---|
| 1 | `ALGORITHM_AND_PROMPT.md` ★ | `hackerthon/` (최상위) |
| 2 | `CONNECT_BOTH_PROMPT.md` | `hackerthon/Connect Both/` |
| 3 | `00_작업서.md` | `hackerthon/appropriate_quantity/` |
| 4 | `01_알고리즘_명세.md` | `hackerthon/appropriate_quantity/` |
| 5 | `02_데이터_명세.md` | `hackerthon/appropriate_quantity/` |
| 6 | `03_연동_인터페이스.md` | `hackerthon/appropriate_quantity/` |

★ 1번이 가장 중요하다. **0.0장(코드 없음 안내) · 부록 O(1단계 점수 산식) · 부록 P(2단계
세부 동작)** 가 이번에 새로 추가됐다. 이 세 부분이 없으면 코드를 재작성해도 값이 달라진다.

---

## B. 데이터 8개

| # | 파일명 | 위치 | 행수(헤더제외) | md5(앞12) |
|---|---|---|---|---|
| 7 | `master.csv` | `appropriate_quantity/` | 743 | `5b6db23876ec` |
| 8 | `txn_history.csv` ★ | `appropriate_quantity/` | 22,625 | `ec2c7b097a1e` |
| 9 | `equipment_map.csv` | `appropriate_quantity/` | 232 | `e3b13eb27a89` |
| 10 | `maintenance.csv` | `appropriate_quantity/` | 20,104 | `f2913368fc11` |
| 11 | `Outbound_Dummy.tsv` | `DOG Type/Dummy Data/` | 30,603 | `93ec08387a7e` |
| 12 | `Inbound_Dummy.tsv` | `DOG Type/Dummy Data/` | 14,679 | `3c4e7632bd0c` |
| 13 | `Stock_Dummy.tsv` | `DOG Type/Dummy Data/` | 12,478 | `8b94611005e4` |
| 14 | `core_spare_parts.csv` | `DOG Type/Core Spare Parts/` | 102 | `712058af8330` |

★ 8번을 빼면 안 된다. 지난번에 이 파일이 빠져 결과가 재현되지 않았다. 2단계 수요원이며
DOG Type TSV로 대체 생성할 수 없다(대체 시 보험품 목표가 79.5억 → 534.5억으로 부풀어 오른다).

**2026-09-10 갱신**: 11·12번(Outbound·Inbound)을 시계열 신뢰성 보강을 위해 기존의 약 3배로
늘렸다(Outbound 10,201→30,603행, Inbound 4,893→14,679행). 늘리는 방식은 같은 (연,월) 안에서
원래 수량을 정확히 나눠 여러 행으로 쪼갠 것이라, 1단계 판정에 쓰이는 파생값(월비율·회전율·
최종활동일)이 수학적으로 보존된다. 실제 `connect_both.py`로 재실행해 743건 판정이 아래 F절
기대값과 **완전히 동일**함을 재검증했다.

---

## C. 보내지 않는 것 — 파이썬 3개 (규칙상 불가)

| 원래 있던 파일 | 대체 규격 |
|---|---|
| `Connect Both/connect_both.py` | `ALGORITHM_AND_PROMPT.md` 부록 O 전체 + 본문 「1단계」·「연결 방식」 |
| `appropriate_quantity/engine.py` | 부록 A~K + 부록 P-1~P-4 · P-7 · P-8 |
| `appropriate_quantity/run_all.py` | 부록 L · M + 부록 P-5 · P-6 |

받는 쪽은 이 규격대로 3개 파일을 새로 작성한 뒤 `PYTHONHASHSEED=0` 을 설정하고
`python -B connect_both.py` 로 실행한다. 자세한 절차는 `ALGORITHM_AND_PROMPT.md` **0.0장**.

## D. 보낼 필요 없는 것

- `*_ver2.tsv` 3개 — 2026-09-10 확장 이전의 원본 상태를 그대로 보존한 **백업본**이다
  (더 이상 현재 Outbound·Inbound와 동일하지 않다 — 의도적으로 안 건드림). 필요 없으면 안 보내도 됨.
- `Connect Both/output/` 의 산출 CSV, `appropriate_quantity/restock.csv`·`pooling.csv`·
  `order_signals_RESULT.csv`·`SUMMARY.txt` — 재현하면 다시 생성된다. 정답 대조는 동료가 이미
  가진 DB 반출본의 `05_속성판정_파생.csv`·`06_적정재고_파생.csv`로 하면 된다.
- `make_dog_dummy_data.py`, `_build_data.py` — 더미 생성기(코드, 반출 불가이며 불필요).
- `*.pdf` 4종, `Outbound RAW.xlsx`, `keys_sample.csv`, `classification_result.csv`(빈 파일).

---

## E. 받는 쪽 읽는 순서

1. `ALGORITHM_AND_PROMPT.md` **0.0장** — 코드가 없다는 전제와 만들 파일 3개 확인
2. 같은 문서 **0.5장** — 재현성 필수사항(PYTHONHASHSEED, ASOF, 입력 스키마)
3. **부록 O** → `connect_both.py` 작성 → 검증(아래 F)
4. **부록 A~K, P** → `engine.py` 작성
5. **부록 L·M, P-5·P-6** → `run_all.py` 작성
6. 나머지 문서(00~03, CONNECT_BOTH_PROMPT)는 배경·데이터 명세 참고용

## F. 재현 검증 기대값

```
classification_result.csv = 743행
판정   보험품 285 · 계획품 294 · 회색지대 31 · 배제-소모품 111 · 배제-순환품 22
판정경로 시계열 점수 607 · 사전 배제 133 · QS 확정 2 · Spare Part 확정 1
신뢰도  HIGH 709 · MEDIUM 3 · LOW 31
보험품점수(si) 관측 최대 85.7   ← 100이 나오면 부록 O-5의 분모(70)를 잘못 쓴 것
```
이 값은 동료의 DB 반출본 `05_속성판정_파생.csv` 743행과 **이미 일치 확인됐다.**

## G. 남은 합의 사항 2건 (알고리즘 ↔ DB)

1. **ASOF 불일치** — 1단계는 `2026-09-07`, 2단계 엔진은 `2026-09-03`, DB 반출본은
   `2026-09-03` 기준. 두 스크립트를 한 값으로 통일할 것. (다행히 1단계 `months` 계산은
   두 날짜 모두 120으로 같아서 현재 판정 결과는 영향받지 않는다.)
2. **승인 시 목표재고 재계산** — 사람이 화면에서 Type을 승인하면 목표재고는 다른 공식으로
   다시 나와야 하는데, 브라우저에는 부트스트랩·수요통계 엔진이 없다. 생성기에서 품목마다
   "보험품 가정 target"과 "계획품 가정 target"을 **둘 다 미리 계산해 파생 CSV에 함께 저장**하면
   화면은 typeSrc에 맞는 값만 고르면 된다(743건이라 계산 부담 없음).
