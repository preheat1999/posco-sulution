# 진척률 정리 · 2026-09-10 기준

포스코 해커톤 본선 「스스로 수량을 자제하는 AI 자재 솔루션」.
4인 팀, GitHub 브랜치 분업(`main` → `dev`/`feat/algo`/`feat/rag`) + 발표자료 1인.

## 한눈에

| 담당 | 브랜치 | 상태 |
|---|---|---|
| 프론트 · 공통 뼈대 (나) | `dev` | 화면 8개 완료 (로그인 · 대시보드 · 속성값 · 적정재고 · 정비계획 · 구매 · 반납 · 현장반납) |
| 속성값·적정재고 알고리즘 | `feat/algo` | **통합 완료** · 결과 CSV 2개 + 요약값을 `algo_in/` 으로 받아 화면에 반영 |
| RAG 챗봇 | `feat/rag` | 데이터 기반 시각화 단계 진행 중, 나머지 완료 |
| 발표자료 | - | 별도 진행 중 |

---

## 1. 공통 데이터층 (완료)

알고리즘 담당이 나와 동일한 구조로 붙일 수 있게 만든 배포용 DB 계층.

- `build_master_db.py` · CSV 11개(더미 743개 자재)를 읽어 브라우저 자산 + 배포용 SQLite/CSV를 **하나의 생성기**로 동시 출력. 검증 리포트 `db/BUILD_REPORT.txt` 자동 생성 (문제 0건).
- **3층 구조 확정**
  - 1층 정본(master) · 원본 스냅샷, 손대지 않음
  - 2층 파생(derived) · 알고리즘 판정 결과
  - 3층 오버라이드(changes, `localStorage`) · 담당자 승인·반납 등 사람 행동, append-only
- `assets/db.js` · 화면이 쓰는 **유일한 진입점**. 3층을 합쳐서(`merge`) 최종값을 계산. `stock`은 저장하지 않고 매번 `스냅샷 + Σ트랜잭션`으로 재계산.
- `db/mtrl_db.py` · 알고리즘팀(Python)이 브라우저와 동일한 로직으로 읽는 리더. 743개 자재 기준 자체 테스트 PASS.
- `db/README.md`, `db/ERD.md` · 알고리즘 담당에게 넘긴 규칙 문서 + ERD(Mermaid).
- `validate_algorithm_csv.py` · 알고리즘팀이 낸 결과 CSV를 743행·PK·enum·수식 재계산까지 10항목으로 검증하는 게이트. 결함 주입 테스트로 검출 확인 완료.

**검증 스크립트 결과**
```
build_master_db.py     : build ok, files=23, problems=0
test_verify_negative.py: 고의 결함 10종 전부 검출
test_db_api.js         : API 11항목 PASS (승인 1건 → 3개 화면 연동 확인)
test_trend.js          : 추이선 16항목 PASS (원천 곡선 10 · 시연용 곡선 6)
test_screens.js        : 화면 8개 전수 PASS (실행 · undefined · 지어낸 값 잔재)
```

## 2. 프론트 화면 (진행 중 · 내 담당)

| 화면 | 상태 | 비고 |
|---|---|---|
| 로그인 (`login.html`) | ✅ 완료 | 2단 레이아웃, POSCO SSO 흐름 UI, 조직 선택기 |
| 대시보드 (`main.html`) | ✅ 완료 | 담당 현황+추이선 · 오늘 할 일 · 도넛 · 감축 비교막대 · 정비마감 · 빠른 실행 타일 · 접이식 2단 |
| 속성값 판단 (`attr.html`) | ✅ 완료 | 원본 → 실행(3초) → **판단**(체크박스 · 전체선택 · 일괄) → **담당자 확정** 4단계 버튼 · 확정 전에는 다른 화면에 반영하지 않는다 · 제외대상 473 · 판단/확정 시각 |
| 적정재고 분석 (`stock.html`) | ✅ 완료 | **분석 실행 버튼** · 핵심예비품 25품목 트리맵(크기=과부족 금액 · 초과 빨강/부족 파랑) · 3·4·5 단계만 · PR 초안 확인창 → 구매신청 이동 |
| 공용 전환 (`pool.html`) | ✅ 완료 | 정체 262품목 · 회수 가능 238 / 보류 24 / 전환 완료 · 확인창 후 3층 기록 |
| 적정구매시점 (`plan.html`) | ✅ 완료 | 이번 주 WO 기본 · WO 머리에 설비명 · 수리계획(선행일수) · 소요 필수 자재 현황 표 · 타임라인 |
| 구매신청 (`purchase.html`) | ✅ 완료 | 4단 파이프라인 · 후보 목록 · AI 채운 칸 표시 + 출처 `?` |
| 자재 확인 · QR 진입 (`material-view.html`) | ✅ 완료 | 자재식별표 QR(`?code=Q0000000`) → 코드 검증 → 식별표 항목 카드 → **반납자(사번 · 이름) 확인** → 확인 팝업 → 모바일 반납 2단계로 인계 |
| 자재반납 (`return.html`) | ✅ 완료 | 상태별 칸 · 반납 실행(재고 재계산) · 효과 미리보기 |
| 현장 반납 (`mobile-return.html`) | ✅ 완료 | 375px 전용 · 큰 스테퍼 · 상태 선택 · 완료 화면 |
| AI 질의 서랍 (`assets/chat.js`) | ✅ 연동 | 사내 RAG(`10.1.14.205:8000`) 스트림 연결 · 진행 4단계 표시 · 글자 실시간 · `[n]` 인용 ↔ 출처 카드 · `no_answer` 경고 톤 · sql 표 · 화면별 추천 질문 · 최근 4턴 맥락. 토큰은 서랍에서 한 번 넣어 브라우저에만 저장(저장소에 없음). **CORS: `localhost:3000`(또는 5173 · 8000)으로 열어야 붙는다** → `python -B serve.py --port 3000` |
| 데이터 근거 (`evidence.html`) | ⏳ 예정 | 심사 임팩트 가장 큰 화면 · **다음 작업** |
| 주간 리포트 (`report.html`) | ⏳ 예정 | 사이드바에 「준비 중」 으로 표시 중 |
| 자재 검색 (`search.html`) | ⏳ 예정 | 사이드바에 「준비 중」 으로 표시 중 |
| QR 인코더 (`assets/qr.js`) | ⏳ 예정 | 지금은 자리표시(「연동 예정」 글자) · 가짜 QR 을 그리지 않았다 |

**공통 뼈대(완료)** · 13개 화면이 공유
- `assets/shell.css` · 토큰·레이아웃·반응형(1180/960/760/640 4단계) 전부 이 한 파일
- `assets/ui.js` · 포맷 함수(금액/날짜/D-day/얼굴아이콘 등) + 공용 아코디언
- `assets/nav.js` · 사이드바 단일 렌더러, 배지 숫자 DB 실시간 반영
- `assets/org.js` · 조직 트리(로그인·리포트 공용)
- `assets/popover.js` · 근거 팝오버 공용 컴포넌트
- `assets/analysis-data.js` · 대시보드 어댑터(화면은 계산하지 않고 DB API만 소비)
- `assets/screen-data.js` · 분석·업무 화면 5개의 어댑터(같은 원칙, DB API만 소비)
- `assets/chat.js` · AI 질의 서랍(RAG 연동 지점)

## 3. 다른 팀원 브랜치

- **`feat/algo` (속성값·적정재고 알고리즘)**: **통합 완료 (2026-09-10)**. `db_output/판정.csv`·`적정재고.csv`·`요약값.json` 을 받아 `validate_algorithm_csv.py` 검문 통과(문제 0건) 후 `algo_in/` 에 두고 재생성. 화면에 뜨는 판정·점수·목표재고·조치가 이제 팀원 알고리즘 결과다. 바뀐 값: 즉시발주 67→61, 정비 마감 「지금 신청」 6→0, 보험품 점수 2행. 나머지(금액·조치·등급·도넛)는 반출 파생값과 동일.
- **`feat/rag` (RAG 챗봇)**: 데이터 기반 응답까지는 구현, **질문에 따른 시각화 단계** 진행 중. 나머지(의존성·모델·외부연결 등 환경 구축)는 완료.
- **챗봇 세 갈래 질의 (2026-09-11)**: 사내 문서만 답하던 챗봇이 **이 서비스의 숫자·화면·용어**도 답한다.
  - **가는 길** · ① `ASK.fast()` 규칙이 잡으면 그 자리에서 (0초) ② 아니면 `serve.py /llm/route` 가 **도구 이름과 값만** LLM 에게 받아 (1.0~1.7초) ③ 도구가 없거나 모르는 값이면 RAG 문서로 (8~12초) ④ 문서에도 없으면 「근거 없음」 경고.
  - **자재 값은 외부로 나가지 않는다** · LLM 에는 질문 문장과 도구 스키마만 보낸다. 숫자는 브라우저가 `DB`·`SCREEN`·`ANALYSIS` 로 만든다. 답마다 「어느 함수 · 어느 필드 · 기준일」 을 근거 줄로 적는다.
  - **도구 13개** (`assets/tools.json`) · 발주/감축 셈 · 금액 요약 · 속성 판단 현황 · 자재 한 건 · 순위 표 · 적정구매시점 · 공용 전환 · 반납 · 오늘 할 일 · 용어 · 화면 · 출처 · 문서.
  - **사전** (`assets/catalog.js`) · 화면 8개 · 용어 27개 · 출처 8개. 값은 안 적고 코드에서 읽으므로 명세가 바뀌면 답도 바뀐다 (`test_ask.js` 가 대조).
  - **키** · `.env` 의 `LLM_API_KEY`·`LLM_MODEL`(claude-haiku-4-5). 서버가 보관하고 브라우저로 내려보내지 않는다.
  - **실사용 확인** · 「지금 발주 필요 몇 품목이야」(즉시·119품목) · 「stale 이 무슨 뜻이야」(설명+지금 값) · 「감축 금액 큰 자재 5개」(표) · 「오늘 할 일」(대시보드와 같은 4줄·633품목) · 「대시보드 설명」 · 「자재 반납 절차」(문서 5건) · 「회식 장소」(근거 없음) 7갈래 통과. 시연 중 새어 나간 세 문장은 `test_ask.js` 에 박아 두었다.
- **음성 · 명령 Agent (2026-09-11)**: 챗봇이 말(🎤)과 글로 받은 **명령**을 실행한다 · 별도 Voice Agent 없이 기존 라우터를 넓혔다. 안내 · `VOICE_GUIDE.md`.
  - **Action Registry** (`assets/actions.js`) · navigate · searchMaterial · openMaterialDetail · getInventoryStatus · openPurchaseRequest · submitPurchaseRequest · openReturnPage · returnMaterial · runAttrAlgorithm · judgeMaterial · commitAttr · runStockAnalysis · convertToPool (13) + `multi_step` 봉투. 화면은 표의 키로만 · 코드는 `Q\d{7}` · 수량 1~999 · **LLM 이 만든 주소 · 코드는 실행되지 않는다**.
  - **DB 변경 5개는 `UI.confirm` 뒤에만** (구매신청 초안 · 반납 · 판단 · 확정 · 공용 전환) · 각 화면의 버튼과 **같은 DB 호출**을 쓴다 (새 저장 경로 없음).
  - **화면 문맥** · 라우터에 `{route, selectedCode, stage}` 를 보낸다 (코드 하나 · 이름 · 금액은 안 감) · 「이 자재」 = 주소 `#q=` → 누른 `[data-q]` → 마지막 말한 코드.
  - **화면을 넘어가는 실행** · 남은 단계를 `sessionStorage` 에 실어 새 화면에서 이어 한다 · 「Q1201564 찾아서 적정재고 화면에서 보여줘」 = 2단계 (`stock.html#q=코드` 새로 지원).
  - **음성** (`assets/voice.js`) · `.env` `OPENAI_API_KEY` 가 있으면 `MediaRecorder` → `serve.py /stt` → whisper (키는 서버) · 없으면 Chrome Web Speech(ko-KR) 로 대체. idle/recording/transcribing/executing 상태 · 인식 문장은 🎤 표시로 채팅에. **마이크는 https·localhost 에서만** 열린다.
  - **실측** · 라우터 1.0~2.1초 · 11개 명령 문장 의도대로 (multi_step 으로 2단계 3건) · 브라우저 실행 · 확인 → 저장 (`pr_drafts` +1) · 취소 → 기록 없음 확인. 검사기 `test_actions.js` 60여 항목 PASS.
- **대시보드 3열 요약판 (2026-09-11)**: 첫 화면을 세 칸으로 다시 짰다 · 무엇을 맡았나(도넛 743 + 변경 제안 · 판단 보류) · 얼마를 쥐고 있나(발주 필요 · 감축 가능 + 보유/목표 견주기 막대) · 언제까지인가(마감 넘긴 넷 + 지금 신청 알림). 세 카드는 [머리 · 몸 · 바닥] 세 층이고 높이를 그리드가 맞춘다.
  - 색 · 반지름 · 치수는 이미 있는 토큰 그대로다 (`shell.css` 1절) · 새 색을 만들지 않았다. Tailwind 는 쓰지 않는다 (2026-09-11 이후 아이콘 글꼴 Material Symbols 은 쓴다 · 아래 항목).
  - 화면에서 뺀 설명은 전부 `?` 안으로 넣었다 · 6갈래 내역 · 목표재고 산식 · 마감일 산식 · 「마감 초과 56건 ≠ 지금 신청 0건」.
  - 「V4.2 ACTIVE · ERP SYNC 09:14」 는 원천에 없어 쓰지 않고, 그 자리에 진짜인 것을 적는다 · 기준일 스냅샷 · 이 화면을 계산한 시각 · 「갱신」(3층이 바뀐 뒤 다시 셈한다).
  - 추이선(담당 현황)과 「오늘 할 일」 은 아래 줄에 남겼다 · 챗봇 `today_tasks` 가 그 값을 가리킨다.
  - 쓰지 않게 된 것은 지웠다 · `donutLegend()` · `finPill()` · `.kpi-viz` · `.viz-*` · `.dashrow.c3`.
- **대시보드를 세 칸으로 줄였다 (2026-09-11)**: 담당 현황(추이선) · 오늘 할 일 · 구매신청·자재반납 진행 · 설비별 자재 현황을 지우고 첫 화면을 [문맥 띠 · 세 칸 · 흐름 띠 · 한 줄]로 끝냈다. 세 번째 칸은 「정비 예정 · 발주 마감」 → **적정구매시점**으로 이름을 바꾸고 데이터도 **이번 주 WO**(`SCREEN.planList('week')`)로 바꿨다 · 대시보드가 따로 세지 않고 적정구매시점 화면과 같은 어댑터를 읽는다(막대는 자재 확보율 · 급한 4건 표시 · 이번 주 6건은 링크로).
  - 부르는 화면이 없어진 어댑터를 지웠다 · `ANALYSIS.dueList` · `pipelines` · `needRows` · `transferable` + 예비 표 둘. `trend` 는 남겼다 (`test_trend.js` 가 지키고 주간 리포트가 쓸 값이다).
  - `main.html` 이 `screen-data.js` 를 읽게 되면서 사이드바 적정구매시점 배지가 plan 화면과 같은 값(이 달 기준)이 됐다 · 전에는 대시보드만 넉 달 전체를 셌다.
- **공용 전환을 왼쪽 메뉴에서 뺐다**: 화면(`pool.html`)은 그대로 있고 적정재고 분석의 「공용화 후보」 와 챗봇에서 들어간다.
- **둘러보기를 여섯 화면 코스로 다시 만들었다 (`assets/tour.js`)**: Dashboard → 속성값 판단 → 적정재고 분석 → 적정구매시점 → 구매신청 → 자재반납. 한 화면의 설명이 끝나면 「다음 화면」 이 실제로 그 화면으로 옮겨 가고 거기서 이어 뜬다(`mtrl.tour.v2` 에 자리를 적어 둔다) · 머리에 여섯 칸 진행 칩을 두고 눌러서 건너뛸 수 있다 · 「이전」 은 앞 화면 마지막 설명으로 돌아간다.
  - 설명은 화면이 실제로 하는 일만 적는다 · 숫자를 적지 않았다(적어 두면 데이터가 바뀔 때 이 파일만 옛말이 된다).
  - 폰 화면(`mobile-return.html` · `material-view.html`)은 PAGE 가 'return' 이어도 자동으로 뜨지 않는다 · 파일 이름까지 맞을 때만 뜬다. `?notour=1` 은 그대로 있다.
- **튜토리얼 · 세 단계 퀘스트 (2026-09-11 · `assets/tour.js` 재작성)**: 로그인하고 대시보드에 들어오면 뜬다 · 25걸음.
  - **시작** 대시보드 세 칸(AI 보유목적 분류 · AI 적정재고 분석 · 적정구매시점) → **1단계 분석** 보유목적 판단 → 적정재고 분석 → **2단계 업무** 적정구매시점 → 구매신청 → 자재반납 → **3단계 질의** AI 자재 질의.
  - **단계마다 문제부터 말한다** · 1단계는 「지금은 감과 관행으로 적정재고량을 정한다」, 2단계는 「시스템마다 데이터가 흩어져 옮겨 적는다」 를 큰 카드로 먼저 띄우고 시작한다.
  - **튜토리얼이 화면을 실제로 움직인다** · 알고리즘 실행 · 적정재고 분석 · 발주 필요 칸 전환 · **PR 초안 1건** · **공용 전환 1건**(확인 창 승인까지) · WO 펼치기 · 초안 열기 · 챗봇 서랍 열기. 각 화면의 버튼과 같은 길이라 3층에만 쌓이고 「시연 초기화」 로 지워진다.
  - **성과 화면** · 단계가 끝나면 암전에 카드만 남기고 숫자를 차례로 크게 올린다(34px) · 1단계는 재고 감축 가능 · 연 금융비용 절감을 **그때 어댑터에서 읽고**, 2단계는 정비 직원 83 FTE(확정 전 · 추정으로 표기) · 한 줄로 이은 정비계획 건수.
  - **퀘스트 달성** · 단계 끝에 `✓ QUEST CLEAR` 배지와 조각 18개, 마지막에는 34개(소리는 쓰지 않는다 · 시연장에서 소리는 사고다).
  - **부드럽게 움직인다** · 판 · 구멍 · 카드를 **한 번만** 만들고 걸음마다 style 만 바꾼다 → 구멍이 0.45초에 이전 자리에서 다음 자리로 미끄러진다(전에는 걸음마다 요소를 새로 만들어 그냥 깜빡였다). 카드도 커졌다 · 설명 460px · 문제 제기 · 성과 · 달성 660px.
  - 화면에서 무언가 실행하는 동안에는 카드에 「화면에서 실행하고 있습니다…」 와 회전 표시를 둔다 · 멈춘 것처럼 보이지 않게.
  - `?notour=1` 은 그대로 · 폰 화면에는 싣지 않는다 · `prefers-reduced-motion` 이면 움직임과 조각을 끈다.
- **같은 와이파이에 HTTPS 로 띄운다 (2026-09-11) · 마이크가 열린다**: `serve.py` 가 HTTP(8130)와 **HTTPS(8443)** 를 한 프로세스에서 같이 띄운다 · 마이크(getUserMedia)와 음성 인식은 **보안 컨텍스트에서만** 열리기 때문이다.
  - 인증서는 이 컴퓨터가 직접 만든다(자체 서명 · `certs/` · **.gitignore**) · SAN 에 이 컴퓨터의 LAN IP · localhost · 127.0.0.1 을 넣는다(SAN 이 없으면 크롬이 아예 열지 않는다). 와이파이가 바뀌어 IP 가 달라지면 다시 만들고, 그대로면 재사용한다(폰에서 경고를 또 넘기지 않게).
  - **확인** · `https://10.1.14.204:8443` 에서 `isSecureContext true` · `getUserMedia` · `MediaRecorder` · Web Speech 전부 열림(헤드리스 크롬으로 대조) · 같은 주소에서 `/rag` 프록시(RAG health 6,901조각)와 `/llm/route`(1.4초)도 그대로 된다. HTTP 로 들어오면 `isSecureContext false` · `getUserMedia` 없음.
  - http 로 들어온 사람에게 화면이 **갈 곳을 알려 준다** · 서버가 `C.SECURE_URL` 을 내려 주고 챗봇 서랍의 마이크 안내가 같은 화면의 https 주소를 눌러 갈 수 있는 링크로 적는다.
  - **접속 QR** · `make_open_qr.py` 가 `qr/open_label.png` 한 장을 만든다 (주소 · 「고급 → 계속」 안내 · 「https 라야 마이크가 열린다」 를 그림 안에 적었다) · 폰 자판으로 주소를 치게 하면 시연이 거기서 끝난다.
  - **다른 기기에서 CSS 가 통째로 안 오던 문제 (2026-09-11 · 고침)** · 그 기기 로그가 중간에서 끊겼다(`shell.css` 는 서버에 도착조차 안 했다). 두 가지가 겹쳤다 · ① 응답이 **HTTP/1.0** 이라 파일마다 연결을 새로 맺고, https 에서는 그때마다 TLS 악수를 했다(파일 20개 = 악수 20번) ② 그 악수를 **듣는 소켓 하나에서 차례로** 했다(listening socket 을 감쌌다) · 한 악수가 늦으면 뒤가 줄줄이 밀리고 폰이 포기한 파일은 로그에 남지도 않는다. 우리 컴퓨터(지연 0.02초)에서는 안 보이고 다른 기기에서만 터진다.
    - 고침 · **HTTP/1.1 keep-alive**(스트리밍 응답만 `Connection: close`) + **연결마다 TLS 악수**(`finish_request` 에서 감싼다 · 깨진 연결 하나는 조용히 버린다) + 끊김 추적을 로그에서 걸러낸다.
    - 확인 · 새 프로필 브라우저로 https 접속 시 요청 25개 전부 200(`shell.css` 포함) · 화면이 제대로 그려진다 · 8연결 병렬 75요청 전부 200 · RAG 스트림 · `/llm/route` 도 그대로.
    - 같이 고친 것 · 좁은 화면에서 상단바의 기준일 · 계산 시각 칩이 제목 위로 접혀 올라오던 것을 900/700px 에서 감춘다(꼭 필요한 것은 「갱신」 과 사람이다).
  - 남은 조건 · 지금 음성→글자는 **브라우저 내장 인식**이라 인터넷이 필요하다. `.env` 에 `OPENAI_API_KEY` 를 넣으면 서버 경유(`/stt`)로 바뀌어 사내망만으로도 돌아간다.
- **발표자료**: 별도 트랙, 이 리포지토리 밖에서 진행.

## 4. 다음 단계

1. `evidence.html` (데이터 근거) · 「0건이 네 개」 를 정면으로 보여 주는 화면. 심사에서 가장 먼저 묻는 지점이라 우선순위 1순위
2. `search.html` · `report.html` · 지금은 사이드바에 「준비 중」 으로 표시 중(눌러도 404 가 안 뜨게 막아 뒀다)
3. `assets/qr.js` · 실제 QR 인코더(버전 3 · 바이트 모드 · ECC L). 지금은 자리표시라 스캔되지 않는다
4. 알고리즘 결과 CSV 수령 → `validate_algorithm_csv.py` 통과 확인 → `feat/algo` 병합
5. RAG 연동 완료 · 시연 순서: `python -B serve.py --port 3000` → `http://localhost:3000` → 추천 질문 클릭 (토큰은 `.env` 에 있어 입력 불필요). 폰(IP origin)에서 쓰려면 백엔드 `allowed_origins` 에 `http://10.1.14.204:8130` 추가 요청 필요

### RAG 연결값 (토큰은 저장소에 없다)

`.env` (gitignore) → `serve.py` 가 읽어 `assets/config.local.js` (gitignore) 로 내려 준다 → 화면이 그것을 읽는다.

```
CHAT_API=http://10.1.14.205:8000
CHAT_TOKEN=<전달받은 토큰>
```

빌드 도구가 없어 브라우저가 `.env` 를 직접 못 읽는다 · 그 다리를 `serve.py` 가 놓는다.
다른 PC · 배포 환경에서 `.env` 가 없으면 서랍에서 토큰을 한 번 넣는 화면이 그대로 뜬다.

**CORS · `serve.py` 가 `/rag/*` 를 RAG 서버로 넘긴다.** 화면은 같은 출처(`/rag`)만
부르므로 CORS 가 생기지 않는다 · **폰(IP 주소)에서도 그대로 된다.** 토큰도 서버가
붙여서 브라우저로 내려가지 않는다. 백엔드의 `allowed_origins` 를 고칠 필요가 없다.
6. `verify.py`(5대 불변식 + 정적 검사) 미작성 · 전체 통합 전 마지막 게이트
   (화면 렌더 확인은 `test_screens.js` 가 대신하고 있다)
