# 연동 인터페이스 — 속성분류 ↔ 적정재고

> 당신이 만드는 **속성값(계획품/보험품) 분류 알고리즘**의 출력이 이 시스템의
> 입력으로 어떻게 이어지는지 규정한다. 이 규격만 지키면 두 시스템이 맞물린다.

---

## 1. 경계 = `classification_result.csv` (master.csv를 직접 덮어쓰지 않는다)

```
 ┌──────────────────────┐  classification_   ┌──────────────────────────┐
 │ 당신의 속성분류 알고리즘 │ ───result.csv────▶ │  이 적정재고 시스템(engine) │
 │ (Qcode+DeptCode →     │  (별도 CSV)        │  Type별 다른 공식으로 산출  │
 │  보험품/계획품/회색지대  │                    │  ※ 보험품·계획품인 행만    │
 │  /배제-*)             │                    │    master.csv Type을 대체 │
 └──────────────────────┘                    └──────────────────────────┘
```

- master.csv의 `Type` 컬럼은 **원본 그대로 두고 직접 고치지 않는다.**
- 대신 별도 파일 `classification_result.csv`에 판정 결과를 저장한다
  (`engine.py`의 `load_classification()`이 이 파일을 읽어 메모리에서만 override한다).
- **`보험품` 또는 `계획품`으로 확정된 행만 master의 Type을 대체한다.**
  `회색지대`, `배제-소모품`, `배제-순환품` 등은 override하지 않고 master.csv의
  원본 Type을 그대로 쓴다 — 판단을 못 내린 품목까지 억지로 바꾸면 안 된다.
- `python -B run_all.py` 실행하면 이 override가 자동 반영되어 산출된다.

---

## 2. 당신이 채울 최소 출력 — `classification_result.csv`

| 컬럼 | 값 | 비고 |
|---|---|---|
| `Qcode` (또는 `Item`) | Q코드 | master.csv와 매칭 |
| `DeptCode` (또는 `부서코드`) | 부서코드 | master.csv와 매칭. 분석키는 `Qcode+DeptCode` |
| `Type` (또는 `판정`) | `보험품` / `계획품` / `회색지대` / `배제-소모품` / `배제-순환품` | 이 값 중 `보험품`·`계획품`만 실제 override로 적용됨 |

**권장(있으면 더 좋음)** — 같은 파일에 함께 제공:
| 컬럼 | 예 | 용도 |
|---|---|---|
| `신뢰도` | HIGH/MEDIUM/LOW | 낮으면 "재검토필요" 표시 |
| `판단근거` | 텍스트 | 분류 근거(감사 추적) |

동일한 `Qcode+DeptCode`가 서로 다른 유효 Type(보험품/계획품)으로 중복되면
`engine.py`가 실행을 중단한다(데이터 정합성 오류로 간주).

---

## 3. Type 이 결과를 어떻게 바꾸나 (오분류 영향)

| 실제 | 분류 결과 | 산출 | 결과 |
|---|---|---|---|
| 보험품 | 계획품(오분류) | 재고 0 | **돌발 시 품절·설비정지 위험** |
| 계획품 | 보험품(오분류) | 상시 안전재고 | **재고 과잉·자금 묶임** |

- **계획품** 분류 시: 상시 재고 0 원칙(핵심설비+소요활발만 소량). 실제 구매는
  수리일정 발주 신호등으로.
- **보험품** 분류 시: 품목 실적 기반 안전재고(μ_LT + SS). 상시 보유.

→ 당신의 분류 정확도가 곧 ①정지방지 ②감축 성능을 좌우한다.

---

## 4. 분류에 쓸 만한 신호 (도메인 지식)

사내 유형 정의:
- **계획품**: 대·중·소수리용 구매. 소요 발견 시점부터 사도 조업 영향 적음.
  → 수리 시점에 몰려 나감, 기계가공품·Valve·Bearing 류.
- **보험품**: 수명예측 곤란·고장 손실 큼. 안전재고 유지 필요.
  → 소요 드물고 불규칙(Lumpy), 핵심설비/핵심예비품 연계, 단가 높음.

관찰된 판별 신호(참고):
- 소요 빈도·규칙성(수요패턴), 핵심설비 연계, 단가, 최근 5년 불출 유무.
- **1.5년 초과 무소요 계획품**은 계획품 자격 상실 → 보험품 전환/공용화 대상.

---

## 5. 재분류 피드백 루프 (양방향)

이 시스템은 운영 중 **재분류 후보를 역으로 알려준다**:
- `output/pooling.csv` — 1.5년+ 정체 자재. 공용화등급 strong/medium/review.
  → 당신의 재분류 입력으로 활용 가능.
- `output/restock.csv` 의 `Reason` — 현재 Type과 실적이 안 맞는 자재 힌트.

→ **당신(분류) → 시스템(산출) → 정체·부적합 탐지 → 당신(재분류)** 순환 가능.

---

## 6. 연동 체크리스트

1. [ ] 각 `Qcode+DeptCode`에 판정 ∈ {보험품, 계획품, 회색지대, 배제-소모품, 배제-순환품} 부여
2. [ ] `classification_result.csv`로 저장 (master.csv는 손대지 않음)
3. [ ] `PYTHONHASHSEED=0` 설정 후 `python -B run_all.py` 오류 없이 실행
4. [ ] `output/restock.csv`의 `TypeSource`로 override가 실제 반영됐는지 확인
5. [ ] `output/SUMMARY.txt` 결과가 합리적인지 확인
6. [ ] (선택) `output/pooling.csv` 로 재분류 피드백 받기

이 5개가 되면 두 시스템은 완전히 맞물린다.
