# 알고리즘 명세 — 적정재고 산출의 모든 공식과 기준

> `engine.py` 와 1:1 대응한다. 이 문서만으로 다른 언어 재구현도 가능하다.
> 목표: 새 데이터를 넣어도 **동일한 기준**으로 판단되게 하는 것.

---

## A. 개요

- 대상: 설비 예비품 743종(보험품 441 / 계획품 302). 기준일 ASOF = **2026-09-03**.
- 목표: ① 재고 감축 ② 설비 정지 방지 (동시).
- **대원칙**: 자재는 `Type`(계획품/보험품)에 따라 **완전히 다른 공식**으로 산출.

```
[데이터] 마스터·불출이력·설비·정비계획
   ▼
[기초] 수요통계 → 리드타임창 부트스트랩 → 중요도등급 S/A/B/C → 안전계수 Z
   ▼
[분기] 계획품: 원칙 0(+핵심설비·활발 예외 소량)
       보험품: 품목실적 기반 μ_LT + 안전재고(SS)
   ▼
[후처리] 정체(1.5년+ 무소요) → 공용화(strong/medium/review)
   ▼
[출력] 적정재고 + 발주 신호등(🔴🟡🟢⚪)
```

---

## B. 기초 지표

### B-1. 수요 통계 (품목별, 부서창고 불출 기준)
부서창고 6개(QFC01/QHB24/QHB25/QHB27/QVC03/QVC07)의 `QTY_OUT` 만 수요로.
- `d_mean` = 총불출 ÷ 기간일수 (일평균 소요)
- `d_std`  = 월소요 표준편차 ÷ √30.44 (일 표준편차 근사)
- `size_mean` = 1회 소요 규모 평균
- `events_yr` = 연 소요 발생 건수
- 수요패턴 = ADI·CV² 기준: ADI<1.32 & CV²<0.49 → Smooth / ADI<1.32 → Erratic
  / CV²<0.49 → Intermittent / 그 외 → Lumpy / 소요없음 → No-Demand

### B-2. 리드타임창 부트스트랩 (lt_samples)
과거 불출을 리드타임(LT) 길이 창으로 400회 무작위 샘플링해 "LT 기간에 나갈 수
있는 소요량" 표본 생성. seed = |hash(품번)| % 99991 (품목별 고정).
- SL분위수 = 이 표본의 서비스수준 분위수
- obs_max = 표본 최대값 (관측상한: 실제 안 나간 양은 안 쌓음)

### B-3. 리드타임 보정
완결사이클 2건 미만이라 LT가 결측/불안정하면: 소싱그룹 중위 → 유형 중위 →
전체 중위(60일)로 대체. σLT는 최소 LT평균×0.25 보장.

### B-4. 중요도 등급 (S/A/B/C)
7요인을 0~1 정규화(백분위) 후 가중합 → 상위 5/20/60% 로 S/A/B/C.
**핵심예비품(CriticalSparePart=O)은 무조건 S 강제.**

**채택 가중치 (train/test 백테스트 최적화 결과)**:
```
F5 공용성 0.50, F1 핵심설비 0.35, F3 리드타임 0.05, F4 대체성 0.04,
F6 고장확률 0.03, F7 조달난이도 0.02, F2 고장영향 0.01
```
요인 정의:
| 요인 | 정의 | 계산 |
|---|---|---|
| F1 핵심설비 | 8h+ 정지 설비 예비품 | (CSP=O?0.5)+(CEQ=O?0.5) |
| F2 고장영향 | 정지 손실 | 정지1일당 정비비 백분위(중위 847,940원) |
| F3 리드타임 | 조달 오래·불안정 | (LT평균+LT표준편차) 백분위 |
| F4 대체성 | 공급사 적음 | 1/공급사수 백분위 |
| F5 공용성 | 타부서 차입 불가 | (부서재고÷전체재고) 백분위 |
| F6 고장확률 | 소요 빈도 | events_yr 백분위 |
| F7 조달난이도 | 수입·단독공급 | (수입?0.5)+(Sole?0.5) 백분위 |

> **왜 F5(공용성) 1순위?** 백테스트에서 "타 부서서 못 빌리는 품목"의 품절이
> 가장 위험 → 공용성을 크게 둘 때 총 품절 최소. (단일요인 100%는 과적합이라
> F1 핵심설비 유지형 채택)

### B-5. 서비스수준·안전계수
| 등급 | SL | Z |
|---|---|---|
| S | 0.995 | 2.58 |
| A | 0.98 | 2.05 |
| B | 0.95 | 1.65 |
| C | 0.90 | 1.28 |
> 등급은 재고 캡(상한)이 아니라 **안전계수 Z 로만** 반영. 실제 재고량은
> 품목별 실적이 정한다.

---

## C. 적정재고 산출 — Type별 분기

### C-A. 보험품 (engine.reserve_target) — 품목별 실적 기반 안전재고
```
소요 이력 없음:
   핵심(CSP/CEQ=O)          → max(설치수기준, 1)
   비핵심 & 초고가(단가>5천만) → 0 (돌발 시 구매)
   비핵심                   → 0
소요 이력 있음:
   μ_LT = d_mean × LT평균
   SS   = Z(등급) × d_std × √LT평균
   norm = ceil(μ_LT + SS),  단 최소 ceil(size_mean) 보장
   obs_max = 부트스트랩 LT창 소요 최대치
   raw  = min(norm, obs_max)          ← 관측 안 된 양은 안 쌓음
   핵심   → max(raw, 설치수기준, 1)
   비핵심 → raw
```
> **핵심 원리**: 등급 캡을 쓰지 않는다. 각 품목의 실적(d_mean, d_std,
> size_mean, obs_max)이 목표를 정하고 등급은 Z 로만 반영. 자주 나가면 자동
> 상향, 드물면 하향. 리드타임 길면 μ_LT가 커져 "조달 느리면 더 많이"가 내재.

### C-B. 계획품 (engine.planned_target) — 원칙 0 + 예외 소량
```
핵심설비 판정 = (CSP=O or CEQ=O)  OR  (연결설비 severity == High)
소요활발 판정 = (최근소요 idle ≤ 1.5년)  AND  (연빈도 λ ≥ 1.0)
             ※ λ = 반감기 4년 최근성 가중 연소요건수

if not 핵심설비:      목표 = 0   (수리일정 발주로만 대응)
elif 최근 3년 무소요:  목표 = 0
elif not 소요활발:     목표 = 0
else:                목표 = max(1, min(SL분위수, 상한 1))   ← 소량
```
> 계획품은 상시 재고 0 이 원칙. 실제 구매는 D장(수리일정 발주)로.

### C-C. 정체 & 공용화 (Type 무관 후처리)
```
최종입고 후 ≥ 548일(1.5년) AND 최근 무소요(idle>1.5년) AND 재고>0 → 정체
공용화 등급:
   전체재고 > 부서재고        → strong (즉시공용화, 위험 없음)
   비핵심/계획품 & 타처 없음    → medium (공용화 권장)
   핵심 보험품 & 타처 없음      → review (유지/재분류 검토)
```

### C-D. 설비 severity (engine._severity)
정비계획에서 설비별:
```
severity = 0.55×정규화(누적정지시간, log1p) + 0.25×정규화(평균인력)
         + 0.20×정규화(대수리·합리화 건수)
상위 25% High / 25~60% Mid / 나머지 Low
```
High 설비에 연결된 계획품은 C-B의 "핵심설비"로 취급.

---

## D. 발주 신호등 (run_all.py)

### 계획품 (수리일정 연동)
```
발주기준일 = 정지시작일 − LT평균 − 선행일수 − 0.5×LT표준편차
   선행일수(휴지구분별): 합리화60 / 대수리45 / 중수리21 / 정기수리14 /
                        교체휴지10 / 공정휴지7
D = 발주기준일 − 오늘
   현재고 ≥ 예상소요 → ⚪ 재고충분
   D ≤ 0            → 🔴 즉시발주
   0 < D ≤ 30       → 🟡 발주임박
   D > 30           → 🟢 여유
예상소요 = 과거 같은 휴지구분 시점 ±30일 불출 평균(없으면 설치수기준/1)
```
### 보험품 (재고포지션)
```
현재고 vs 목표재고:
   목표 0            → ⚪ 재고불요
   현재고 < 목표×0.5 → 🔴 즉시발주
   현재고 < 목표     → 🟡 발주임박
   현재고 ≥ 목표     → ⚪ 충분 / 🟢 여유
```

---

## E. 재무효과

```
금융비용 절감 = (자동발주 감축액 + 공용화 회수액) × 기여율(0.20) × 이자율(0.046)
전사 확산: 재고감축목표 2,016억 × 20% × 4.6% = 연 18.5억원
```

---

## F. 검증 방법 (선택)

- train/test 분할: 불출이력 앞 70%로 목표 산출, 뒤 30%를 하루씩 시뮬레이션.
- 품절 판정: 매일 현재고가 그날 QTY_OUT 못 채우면 품절. 1회 상한 30일.
- **품목 중심 지표**로 평가: 품목별 품절일수·품절률의 평균. "하루라도 품절난 날
  비율"은 착시라 쓰지 않는다.
- 기대: 품목당 평균 품절률 약 0.9%(서비스수준 99%대), 핵심 보험품 90% 무품절.

---

## G. 재현 시 유의 (이 패키지 vs 원본 미세차)

- 원본은 F2(고장영향)에 ERP 실측 정비비 파일을 썼으나, 그 파일이 보안(DRM)
  때문에 이 패키지엔 없어 **중위값 상수**로 대체했다. 이로 인해 중요도 등급
  분포가 아주 약간 달라져 보험품 목표총액이 원본(약 60억)과 소폭 차이날 수 있다.
- 방향·규모·핵심 결론(계획품 −99.8%, 핵심품 보호, 공용화 여력)은 동일하다.
- 실측 정비비 파일을 확보하면 `engine.criticality` 의 F2 를 그 값으로 바꿔
  원본과 완전히 일치시킬 수 있다.
